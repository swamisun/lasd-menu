[project]
name = "lasd-menu"
version = "0.1.0"
description = "Vegetarian and vegan menu calendar built from Los Altos School District menu PDFs"
requires-python = ">=3.11"
dependencies = ["icalendar>=6", "pdfplumber>=0.11"]

[tool.uv]
package = false
